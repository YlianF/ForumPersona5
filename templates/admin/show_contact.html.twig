{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
<div class="content">
    <h1>Contact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ contact.auteur.email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact.message }}</td>
            </tr>
        </tbody>
    </table>

    <br>

    {{ include('admin/_delete_form_contact.html.twig') }}

    <br>


    <br>

    <a href="{{ path('app_admin_index_contact') }}">retour à la liste</a>

    
</div>
{% endblock %}